name: flutter_whatsapp_project
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  chat_bubbles: ^1.6.0
  contacts_service: ^0.6.3
  firebase_core: ^2.24.2
  flutter:
    sdk: flutter
  flutter_application_1: ^0.0.2
  image_picker: ^1.0.7
  permission_handler: ^11.0.1
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.4.9

flutter:
  uses-material-design: true
  assets:
    - assets/background_image.webp